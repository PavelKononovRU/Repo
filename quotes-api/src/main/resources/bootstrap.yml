server:
  port: 0
spring:
  application:
    name: quotes-api
  cloud:
    config:
      profile: local
      discovery:
        enabled: false
        service-id: config-server
  data:
    redis:
      host: localhost
      port: 6379
  main:
    allow-bean-definition-overriding: true