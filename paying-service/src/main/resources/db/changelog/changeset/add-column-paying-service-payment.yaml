databaseChangeLog:
  - changeSet:
      id: add-column-paying-service-payment
      author: Misha
      changes:
        - addColumn:
            tableName: payment_table
            columns:
              - column:
                  name: user_id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    nullable: false
                    foreignKeyName: users
                    references: users(user_id)